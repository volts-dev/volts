package test

type (
	Args struct {
		Num1 int
		Num2 int
		Str  string
		Flt  float64
	}

	Reply struct {
		Num int
		Str string
		Flt float64
	}

//	Arith interface {
//		Mul(hd *router.TRpcContext, args *test.Args, reply *test.Reply) error
//	}
)
